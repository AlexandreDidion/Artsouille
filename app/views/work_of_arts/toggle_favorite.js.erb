var work_of_artId = '<%= @work_of_art.id %>'

function toggleLikeButton(work_of_artId) {
    let likeButton = document.querySelector(`.favorite-icon[data-id='${work_of_artId}']`)
    likeButton.classList.toggle('red');
  }
  toggleLikeButton(work_of_artId);