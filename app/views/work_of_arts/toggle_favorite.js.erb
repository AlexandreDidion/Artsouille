var work_of_artId = '<%= @work_of_art.id %>'

function toggleLikeButton(work_of_artId) {
    let likeButton = document.querySelector(`.favorite-heart[data-id='${work_of_artId}']`)
    likeButton.classList.toggle('favorited');
  }
  toggleLikeButton(work_of_artId);