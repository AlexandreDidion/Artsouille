<div class="container">
  <div class="row justify-content-center">
    <div class="col-6">
      <%= simple_form_for [@to_do, @to_do_item] do |f| %>
        <%= f.input :content %>
        <%= f.button :submit, class:"btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
