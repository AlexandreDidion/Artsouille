<%= @user.username  %>

<%= simple_form_for @user do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :description %>
  <%= f.button :submit, class:"btn btn-primary" %>
<% end %>
